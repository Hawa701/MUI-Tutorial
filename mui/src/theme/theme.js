import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#1760a5",
      default: "green",
    },
    secondary: {
      main: "#15c630",
    },
    background: {
      default: "#303030",
    },
    text: {
      default: "white",
    },
  },
});
